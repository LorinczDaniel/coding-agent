# docs/

`superpowers/` contains **historical planning artifacts** — point-in-time
specs and implementation plans from early feature work (TUI, system prompt,
glob/grep tools, model switching). They are kept for reference and do not
describe the current design; the source of truth is the code, README.md, and
CLAUDE.md at the repo root.
