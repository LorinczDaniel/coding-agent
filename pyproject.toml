[project]
name = "claude-agent"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "openai>=2.15.0",
    "python-dotenv>=1.0.0",
    "textual>=0.80.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
